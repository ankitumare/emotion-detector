from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Production-grade ML pipeline with DVC for text sentiment analysis',
    author='ankit umare',
    license='',
)
